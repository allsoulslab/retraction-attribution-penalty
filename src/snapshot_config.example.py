"""Local configuration for the snapshot reader.

Copy this file to `snapshot_config.py` and set SNAPSHOT_ROOT to the path of the
local OpenAlex snapshot. `snapshot_config.py` is gitignored and is not part of
the repository, so machine-specific paths are never committed.
"""

SNAPSHOT_ROOT = "/path/to/openalex_snapshot"
